.DS_Store
.ipynb_checkpoints/
__pycache__/
*.pyc

# Temporary notebooks produced by scripts/run_parameters.py.
tmp_run_*.ipynb
executed_notebooks/

# Legacy one-off output folders from earlier notebook versions.
G_r*_visualization/
G_r*_matrices/

# New runs write here.  The repository keeps a few curated sample outputs
# already tracked in git; fresh parameter runs are ignored by default so large
# local experiments do not get committed accidentally.
figures/G_r*_k*_h*/
matrices/G_r*_k*_h*/
